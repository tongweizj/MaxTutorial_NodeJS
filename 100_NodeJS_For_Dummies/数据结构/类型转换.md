# 数据类型转换

## str 转成 Int

```JS
var a = parseInt("007");
```

### 进制

The function call looks like:

```JS

parseInt(string, radix);
```

And here's an example:

```JS
var a = parseInt("11", 2);
```

binary number  二进制